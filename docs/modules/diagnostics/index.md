# Diagnostics

Zero-cost developer tracing via `Senf.Tracing` and an `ActivitySource` for OpenTelemetry. When no listener is configured, all trace calls are no-ops.

## Pages in this section

- [**Tracing**](./tracing) — `Senf`, `ServusTrace`, `TraceChannel`, `TraceEvent`, `TraceLevel`, `IServusTraceListener`, `ServusTraceExtensions`.

## Namespace map

| Namespace | Types |
|---|---|
| `Servus` | `Senf` |
| `Servus.Diagnostics` | `ServusTrace`, `ServusTraceExtensions`, `TraceChannel`, `TraceEvent`, `TraceLevel`, `IServusTraceListener`, `ServusMetrics` |
