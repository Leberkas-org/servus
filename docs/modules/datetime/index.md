# DateTime

A short list of `DateTime` extensions for the predicate checks you end up writing by hand: "is this today?", "is this between X and Y?", "is this a workday?". Tiny, but they make call sites read better.

## Pages in this section

- [**Extensions**](./extensions) — `IsToday`, `IsBetween`, `IsWorkday`, `IsWeekend`, `IsInFuture`, `IsPast`.

## Namespace map

| Namespace | Types |
|---|---|
| `Servus` | `DateTimeExtension` |
